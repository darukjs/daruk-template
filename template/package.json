{
  "name": "{{name}}",
  "version": "1.0.0",
  "description": "{{description}}",
  "main": "master.js",
  "scripts": {
    "build": "rm -rf ./dist && tsc && cp -r ./src/view ./dist/src/view && cp -r ./src/public ./dist/src/public",
    "dev": "NODE_ENV=dev nodemon --exec ts-node --files master.ts",
    "prod": "pm2 start ./pm2.config.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "{{name}}",
    "{{description}}"
  ],
  "author": {
    "name": "{{author}}",
    "email": "{{authorEmail}}"
  },
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^1.18.3",
    "ts-node": "^7.0.1",
    "typescript": "^3.0.1",
    "mocha": "^5.2.0",
    "tslint-config-prettier": "^1.17.0",
    "tslint-config-alloy": "^0.2.1",
    "tslint": "^5.11.0"
  },
  "dependencies": {
    "@types/node": "^8.0.53",
    "@sina/daruk": "https://gitlab.weibo.cn/daruk/daruk.git#refactor"
  }
}
